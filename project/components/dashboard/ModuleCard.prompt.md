Service-category card rendered as a dashboard module, not a brochure card — count reads like a live module count.

```jsx
<ModuleCard icon={<Icon/>} title="GST Compliance" description="Registration, returns, reconciliation." count={14}/>
```
Used on the Services Hub to render each of the 10 categories.
