One CLI-style result row for `TerminalSearch` output — a service name, a status word, and a `[ book ]` command instead of a button.

```jsx
<CommandOutputLine label="gst-registration" status="available" onBook={openWhatsapp}/>
```
