Primary site header — logo, brand wordmark (last word gold), nav links with an underlined active state, phone number.

```jsx
<NavBar logoSrc="/assets/logo.jpg" items={[{label:'Home',href:'/',active:true},{label:'Services'}]} phone="+91 7890594600"/>
```
